iam:
  image:
    path: "/deps-platform/deps-iam-api"
  resources:


iam_consumer:
  image:
    path: "/deps-platform/deps-iam-api"

polling_publisher:
  image:
    path: "/deps-platform/deps-iam-polling-publisher"

migrator:
  image:
    path: "/deps-platform/deps-iam-migrator"

fixtures:
  image:
    path: "/deps-platform/deps-iam-migrator"

iam_settings:
  ENV: other

  DATABASE_REQUIRE_SECURE_TRANSPORT: false

migration_settings:
  DATABASE_REQUIRE_SECURE_TRANSPORT: false

  helm:
  helm_logs_job_enabled: false

csi:
  vault_namespace: 'k8s-other'
  roleName: 'k8s-other'
  vaultKubernetesMountPath: 'kubernetes-other'

service_account:
  serviceAccountName: 'deps-other'
